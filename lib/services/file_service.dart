import 'package:phinfo/models/device_info.dart';

class FileService {
  String path;
  static String state;

  static generateFile(DeviceInfo deviceinfo) async {

    Direcrory directory = await getApplica
  }
}
